const height = [65, 66, 72, 66, 60, 77];

function getMax(numbers) {
    let max = numbers[0];
    for (number of numbers)
    {
        if (max < number) {
            max = number;
        }
    }
    return max;
}
const max = getMax(height);
console.log("Max value is: ", max);